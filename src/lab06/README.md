# Лабораторная работа №6: CLI-утилиты на Python

## 📋 Описание
Этот проект реализует инструменты интерфейса командной строки (CLI) с использованием встроенной библиотеки Python `argparse`. Цель проекта — создание модульных утилит для автоматизации повседневных задач. Проект разделен на два основных модуля:

1.  **Анализ текста (`cli_text.py`)**: Чтение файлов и статистический анализ частоты слов.
2.  **Конвертация данных (`cli_convert.py`)**: Преобразование форматов данных между JSON, CSV и Excel (XLSX).

## 📂 Структура проекта

```text
python_labs/
├─ README.md                   # Отчет по проекту
├─ requirements.txt            # Список зависимостей
├─ src/
│   ├─ lab06/
│   │   ├─ cli_text.py         # Инструмент для текста (команды cat, stats)
│   │   ├─ cli_convert.py      # Конвертер данных (json2csv, csv2xlsx и др.)
│   │   └─ __init__.py
├─ data/
│   ├── samples/               # Входные файлы (people.json, text.txt)
│   └── out/                   # Файлы, сгенерированные скриптами
└─ setup_data.py               # Скрипт для генерации тестовых данных
⚙️ Установка и ТребованияДля работы проекта требуется Python 3.x.Для функции экспорта в Excel необходимо установить библиотеку openpyxl.Bashpip install openpyxl
🚀 Настройка тестовых данныхДля создания примеров файлов (включая демографические данные по Анголе и Нигерии), выполните скрипт настройки:Python# Фрагмент setup_data.py
import json, os
os.makedirs("data/samples", exist_ok=True)

people = [
    {"name": "Emeka Okonkwo", "role": "Engineer", "age": "30", "country": "Nigeria"},
    {"name": "Joao Baptista", "role": "Designer", "age": "25", "country": "Angola"},
    {"name": "Nneka Adebayo", "role": "Manager", "age": "40", "country": "Nigeria"},
    {"name": "Ana Mateus", "role": "Data Analyst", "age": "28", "country": "Angola"}
]
with open("data/samples/people.json", "w", encoding='utf-8') as f:
    json.dump(people, f, indent=2, ensure_ascii=False)
💻 Инструкция по запускуВыполняйте команды из корневой папки проекта.1. Инструменты для текста (cli_text.py)Команда stats (Статистика слов)Анализирует частоту слов в текстовом файле.Bashpython src/lab06/cli_text.py stats --input data/samples/text.txt --top 3
Ожидаемый результат: Список из 3-х самых часто встречающихся слов.Команда cat (Чтение файла)Читает и выводит содержимое файла в консоль. Используйте флаг -n для нумерации строк.Bashpython src/lab06/cli_text.py cat --input data/samples/text.txt -n
2. Конвертеры данных (cli_convert.py)JSON в CSVКонвертирует список людей (формат JSON) в таблицу CSV и выводит результат в терминал для проверки.Bashpython src/lab06/cli_convert.py json2csv --in data/samples/people.json --out data/out/people.csv
CSV в JSONКонвертирует CSV файл обратно в формат JSON.Bashpython src/lab06/cli_convert.py csv2json --in data/out/people.csv --out data/out/people_converted.json
CSV в Excel (XLSX)Генерирует совместимую таблицу Excel.Bashpython src/lab06/cli_convert.py csv2xlsx --in data/out/people.csv --out data/out/people.xlsx
📊 РезультатыСконвертированные файлы сохраняются в директорию data/out/.Пример данных, которые будут в файле Excel (people.xlsx):nameroleagecountryEmeka OkonkwoEngineer30NigeriaJoao BaptistaDesigner25AngolaNneka AdebayoManager40NigeriaАвтор: [Ваше Имя]Дисциплина: Программирование на Python - Лабораторная №6